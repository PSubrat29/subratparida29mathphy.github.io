# GitHub Pages configuration
# This file tells GitHub Pages how to serve your site

# Since we're using pure HTML/CSS (no Jekyll), disable Jekyll processing
exclude:
  - CV_DATA_TEMPLATE.json
  - README.md

# Security headers are configured in .github/workflows/deploy.yml
# and via inline meta tags in HTML
